# Project Setup Notes

## What We Did

- Confirmed this project folder started empty.
- Installed Blender via Homebrew:

```sh
brew install --cask blender
```

- Launched Blender from the terminal.
- Initialized this folder as a Git repository.
- Connected it to the GitHub remote:

```sh
git@github.com:donkefant/test.git
```

- Created the initial commit and pushed `main`.

## Handy Commands

Launch Blender:

```sh
blender
```

Or:

```sh
open /Applications/Blender.app
```

Push changes:

```sh
git add .
git commit -m "your message"
git push
```
